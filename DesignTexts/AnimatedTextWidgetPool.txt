I need you to setup a subsytem, component, new actor class and add functions to mission manager and mission base for the following new feature:

trigger volumes that have callbacks registered with specific missions.
we need a new component that will be added to the mission manager at the ctor called MissionTriggerVolumesManager
this one will keep track of which TriggerArea is setup with which mission base.

you need to create the ATriggerArea actor class which in post init needs to look for its collision component can be sphere or box collision.

you need to add a new DeveloperSettings class for allowing the designer to set the classes that are derived actors from ATriggerArea
one for the rectangle one and one for the sphere one.

I want the mission base to be able to call wrapper functions that will create these trigger Area actors and have the following params,
 there will be two versions CreateTriggerAreaSphere and CreateTriggerAreaRectangle.
 the will get a location, frotator, a scale FVector (to apply scale to either the sphere or box collision component on the actors to be created),
 a ETriggerOverlapLogic to determine whith which objects the trigger volume should trigger overlap events.
 a float DelayBetweenCallbacks; for how long between callign the callback again on the mission base;
 keep track of the time of when the last one was; if the next trigger is too early then ignore it.
 int32 MaxCallbacks; for how many times the callback should be called before the trigger volume is destroyed, <0 for infinite.
 integer ID; for the ID on the callback of the mission.
 
 the callbacks on the mission should have the following signature; first calling a c++ function
 with AActor* OverlappingActor, int32 TriggerID, ATriggerArea* TriggerVolume.
 
 make sure that the cpp callback calls a BP_ callback of it, like we do for many of the other callback mechanics on the mission base.
 
 make sure that the new component will load the classes from the DeveloperSettings at beginplay and use them when creating the trigger area actors.
 
 IMPORTANT NOTE: when a volume has provided the param OverlapPlayer for ETriggerOverlapLogic then make sure it ovelaps
 only  COLLISION_OBJ_PLAYER. but when it is set to overlap enemy then only overlap objects of COLLISION_OBJ_ENEMY
 and in case of OverlapBoth overlap both.
 
 also add functions to remove trigger volumes by ID and mission base associated with them; trigger volumes of different missions should be allowed to have the 
 same ID. also multiple IDs for the same volumes for one mission should be allowed; in  that case destroy all of those  that have that ID.
 
 make sure that when a mission completes that has trigger volumes assosiated that then these volumes are removed automatically.
 
 fully implement the new feature.
 
