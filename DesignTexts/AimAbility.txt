You are going to implement a new EAbilityID called IdSupportUnit

it will also need its own enum to differentiate different support abilities, called ESupportAbilityType
which will be used like how we use other abilities with sub enums as well. (see behaviour ability for example)

This will essentially be a support ability where one unit targets another allied unit and provides support. I want to keep 'abstraction hooks' so that the effect / what it looks like when a support unit supports another can be filled in at blueprint level 
so we need bp events like
BP_OnSupportingTank(ATankMaster* AlliedTank)
BP_OnStopSupportingTank (ATankMaster* AlliedTank ) 
BP_OnSupportingSquad(ASquadController* AlliedSquad)
BP_OnStopSupportingSquad (ASquadController * AlliedSquad ) 

I think it would be best if those bp events are part of a new blueprint-friendly interface called UnitSupporter

this abiltiy ID needs to be driven by a component like aim abilities, behaviour abilities have as well so we add the component which then has 
settings for the ability index and cooldown, in this case also add settings for resources in case the designer wants the support ability 
to cost more than just time.

we also need some kind of flag system so that the designer can easily set SupportTargets in a data type called that name.
Where possible targets are : Buildings (BuildingExpansions, Expanded Nomadic trucks) , Tanks (but not expanded nomadics; only in  truck form!),
Squads

so note that aircraft are both not support units nor targets for any support units!

also a bp friendly specific support ability struct with:
the SupportTargets datatype.
float SupportRange


