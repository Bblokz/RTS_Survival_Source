You need to add a new feature to the mission manager that is callable from the mission base.
it will concern making choices for which arrays of units to spawn depending on the campaign generation seed.

here is what i want:
esentially the function call will get an array of FSeededChoices where each FSeededChoices has multiple choices;
a choice is itself a struct containing an array of FseededOptions which contains all the options the seeding can select from.an array of  FVectors and associated soft object ptrs to AActors, those concern actors that do not need to 
an FSeededOption has
be spawned through the async spanwer but rather are spawned asynchronously through calling the assetloader directly
