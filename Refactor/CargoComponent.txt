we want to refactor the way strength estimation works and how certain data related to strength estimation is cached on objects. 
First: we will divide strength percentage up into three categories now called EWorldStrengthTypes, see also WorldStrengthTypes.h, 
-FortificationStrength, 
-FieldDivisions, 
-StrategicSupport 

Where fortification strength will be composed of two factors: the base strength as determined by the type of object; loaded from the world data asset and multiplied with its modifier for game difficulty (just like before)
and from enums laoded from a new actor component that will be created in the ctor of map object: FortificationModifications which will hold an array of EWorldFortificationStrength.

make sure that on the world data asset there will be a category to get the int32 percentage per EWorldFortificationStrength and a multiplier per difficulty for that category.

I also want the base strength to be cached on that component so no longer directly on the AWorldEnemyObject or the AWorldMissionObject but on the component both of these classes now have due to their common base.

How we will also refactor AuxiliaryDifficultyInfluenceReasons
the UWorldDifficultyInfluence will now always be of the type StrategicSupport, make sure that in the component edit anywhere the designer 
will set the type of EWorldStrategicSupport. the world difficulty influence was already renamed to UWorldStrategicSupportArea.
I want AdjustDifficutlyPercentagesForInfluencers renamed to AdjustDifficultyPercentagesForStrategicSupport and in there ResetStrategicReport instead of ResetAuxiliaryDifficultyInfluenceReasons
and ApplyStrategicSupport instead of ApplyDifficultyInfluencers.


also add to the AGeneratorWorldCampaign AdjustDifficultyPercentagesForFieldDivisions and in there call a reset and apply for that type as well but leave those implementations empty; we will implement field divisions later.

on the UW_StrengthEstimation when will make sure that then the strength percentages and their reasons are put in the correct rich text blocks:

M_FortificationsRichText: the base difficulty and those from the fortification modifiers.
M_FieldDivisionsRichText: for the field divisions nearby (empty for now; only setup empty function calls as we will implement it later)
M_StrategicSupportRichText the