You are a c++ expert and perfectionist.
You are going to add new logic to the mission manager to support a certain check system that will, when the check find it is allowed
execute a global abiltiy that is provided with it (save all of its state of this particular check in a struct all GC safe, but in a way it does not
clutter the mission manager too much)

New ticking logic for mission manager:
Global Ability On lost all units of type

first define in its own file; maybe together with the struct that holds state for one of these checks;
EReinforcementAirdropLocation
NotSet,
OverrideLocation,
CommanderVehicleLocation,
HQLocation

THis feature will issue a global abilty provided for it and execute it either at the override location or otherwise at the location
of the command vehicle of the player or their HQ with some offset in those cases in XY to not be on top.
it will be set up with either a
ESquadsubtype or Etanksubtype or Enomadic subtype
to check, what it will do:
0) make sure the global ability is initialized.
1) in tick check with the game unit manager if the player still has any of the unit(s) of the provided subtype.
if yes nothing happens
2) the momeent the player has non remaining of the unit we execute the abiltiy at either the ovrride location or
at the HQ or commander position with some XY offset which needs to be a setting.

if we do HQ or commander position make sure that after retreiving those positions we add the XY offset.
if these are not available pick the location of any tank owned by the player and add the offset to that and continue with that as 
the location.
project the target location to the navmesh with the rts funtion library and extent 5 and use that location for the provided global ability.
execute the provided global ability at the location.

make sure  to bind to the OnGlobalAbilityEnded before executing safely.

also add a setting for this called bool bRenablePostGAComplete which is default set to false.
While the ability is executing the tick check for this instance is always paused ; does nothing regardless of
bRenablePostGAComplete.
once the abiltiy lets this tick check know it is done executing and bRenablePostGAComplete is false then remove the abilty and this tick
check entirly else if bRenablePostGAComplete is true; renable the tick check and the whole thing works again like before.