I now want to implement the architecture for the difficulty percentage modifiers.
we already have UI data setup like Strength percentage text on Map item UI data. and Influence reasons. The idea is that the designer will be able to define all percentages and radii (explained later) in our WorldData data asset.

Important to define first is a GameDifficultyMultiplier which allows the designer to define float multipliers for each of 
the possible game difficulties.
this multiplier will appear in various sections so ensure it is well defined in its own file.

First the base difficulty percentages:
make a section on WorldData  where the designer can set the base difficulty int percentage for each mission enum and 
define one GameDifficultyMultiplier for just the missions base dificulty. The base difficulty calculated will then be
the value for the enum multiplied by the multiplier for the game difficulty.

DifficultyModifierRadius will be an actor component of which the reference will be placed on the enemy world object and the component
will be looked for at post init components.
it will have its own settings struct with;
int32 AddedDifficulty
and float XYRange
 